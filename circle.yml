machine:
  timezone: Europe/Berlin

general:
  branches:
    only:
      - master
  build_dir: build

compile:
  override:
    - pwd
    - ls -l
    - mkdir build
    - cd build
    - cmake ..
    - make

test:
  override:
    - pwd
    - ls -l
    - mkdir build
    - make test
