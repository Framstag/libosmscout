machine:
  timezone: Europe/Berlin

general:
  branches:
    only:
      - master

compile:
  override:
    - pwd
    - ls -l
    - mkdir build
    - cd build
    - cmake ..
    - make

test:
  override:
    - pwd
    - ls -l
    - mkdir build
    - make test
