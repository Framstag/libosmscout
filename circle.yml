machine:
  timezone: Europe/Berlin

general:
  branches:
    only:
      - master

compile:
  override:
    - pwd
    - ls -l
    - mkdir build && cd build && cmake .. -DOSMSCOUT_BUILD_BINDING_JAVA=OFF && make

test:
  override:
    - pwd
    - ls -l
    - cd build && make test
