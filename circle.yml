machine:
  timezone: Europe/Berlin

general:
  branches:
    only:
      - master

compile:
  override:
    - mkdir build && cd build && cmake .. -DOSMSCOUT_BUILD_BINDING_JAVA=OFF && make

test:
  override:
    - cd build && ctest
