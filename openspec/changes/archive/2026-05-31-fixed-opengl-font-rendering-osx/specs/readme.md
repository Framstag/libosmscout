## ADDED Requirements

(none — this change modifies no public API or interface contracts)

This fix is purely an implementation change: a new internal utility function `UTF8StringToU32StringLE` added to `libosmscout` and used by the OpenGL text renderer. No spec-level capabilities are introduced or modified.