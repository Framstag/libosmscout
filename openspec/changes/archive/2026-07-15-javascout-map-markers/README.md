# javascout-map-markers

Show favorites and selected search result on map using existing map rendering pipeline (poiNodes). Adds custom POI types for favorite locations and search selection, styled via .oss files.
