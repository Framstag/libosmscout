# Tasks: man-made-types-and-styles

## 1. Type definitions in map.ost

- [x] 1.1 Add way-only TYPE definitions (`man_made_cutline`, `man_made_pipeline`, `man_made_embankment`, `man_made_goods_conveyor`, `man_made_cooling`, `man_made_snow_fence`) to the man made section of `stylesheets/map.ost` (spec: Way-only man made types) and verify each type name is unique in the file
- [x] 1.2 Add node-only TYPE definitions (`man_made_surveillance`, `man_made_manhole`, `man_made_mast`, `man_made_utility_pole`, `man_made_survey_point`, `man_made_petroleum_well`, `man_made_flagpole`, `man_made_chimney`, `man_made_beehive`, `man_made_charge_point`, `man_made_cross`, `man_made_adit`, `man_made_lighthouse`, `man_made_snow_cannon`, `man_made_flare`, `man_made_beacon`, `man_made_telephone_box`, `man_made_oil_gas_separator`, `man_made_buoy`, `man_made_fuel_pump`, `man_made_carpet_hanger`) to the man made section of `stylesheets/map.ost` (spec: Node-only man made types) and verify each type name is unique in the file
- [x] 1.3 Add area-only TYPE definitions (`man_made_clearcut`, `man_made_tunnel`, `man_made_spoil_heap`, `man_made_tailings_pond`) to the man made section of `stylesheets/map.ost` (spec: Area-only man made types) and verify each type name is unique in the file
- [x] 1.4 Add node+area TYPE definitions (`man_made_storage_tank`, `man_made_tower`, `man_made_water_well`, `man_made_silo`, `man_made_street_cabinet`, `man_made_works`, `man_made_water_tower`, `man_made_water_tap`, `man_made_bunker_silo`, `man_made_monitoring_station`, `man_made_reservoir_covered`, `man_made_heap`, `man_made_planter`, `man_made_water_works`, `man_made_pumping_station`, `man_made_kiln`, `man_made_courtyard`, `man_made_crane`, `man_made_gasometer`, `man_made_mineshaft`, `man_made_watermill`, `man_made_cairn`, `man_made_windmill`, `man_made_windpump`, `man_made_clarifier`, `man_made_dolphin`, `man_made_satellite_dish`, `man_made_stupa`, `man_made_communications_tower`, `man_made_bioswale`, `man_made_column`, `man_made_telescope`) to the man made section of `stylesheets/map.ost` (spec: Node and area man made types) and verify each type name is unique in the file
- [x] 1.5 Add way+area TYPE definitions (`man_made_breakwater`, `man_made_groyne`, `man_made_dyke`, `man_made_quay`) to the man made section of `stylesheets/map.ost` (spec: Way and area man made types) and verify each type name is unique in the file
- [x] 1.6 Add node+way TYPE definitions (`man_made_advertising`, `man_made_gantry`) to the man made section of `stylesheets/map.ost` (spec: Node and way man made types) and verify each type name is unique in the file
- [x] 1.7 Add node+way+area TYPE definitions (`man_made_antenna`, `man_made_avalanche_protection`, `man_made_ceremonial_gate`, `man_made_geoglyph`) to the man made section of `stylesheets/map.ost` (spec: Node, way and area man made types) and verify each type name is unique in the file
- [x] 1.8 Verify no TYPE definitions were added for discouraged/undocumented values (`man_made=yes`, `man_made=lamp`, `man_made=pumping_rig`, `man_made=launch_pad`, `man_made=pond`, `man_made=tar_kiln`, `man_made=marsh_terrace`, `man_made=waterway`, `man_made=pole`) by grepping `stylesheets/map.ost` for the corresponding type names (spec: Discouraged and undocumented values are excluded)

## 2. Style definitions in man_made.oss

- [x] 2.1 Add area fill rules for the new industrial/ground-disturbance/water-structure area types in the `[MAG detail-]` block of `stylesheets/include/man_made.oss` (spec: Style definitions for new man made types) and verify the rules reference `@industrialColor` or dedicated colors
- [x] 2.2 Add WAY line rules for the new linear types (`cutline`, `pipeline`, `embankment`, `goods_conveyor`, `cooling`, `snow_fence`, `breakwater`, `groyne`, `dyke`, `quay`, `advertising`) in the `[MAG close-]` block of `stylesheets/include/man_made.oss` (spec: Style definitions for new man made types) and verify the rules reuse the pier/dam rendering patterns
- [x] 2.3 Add NODE.ICON rules for the new node types in the `[MAG veryClose-]` block of `stylesheets/include/man_made.oss` (spec: Style definitions for new man made types) and verify each rule references a symbol defined in the man_made module
- [x] 2.4 Add the new SYMBOL definitions (tower, communications tower, mast, water tower, lighthouse, windmill, watermill, windpump, cross, cairn, column, stupa, ceremonial gate, chimney, flare, crane, gasometer, storage tank, silo, flagpole, beacon, buoy, dolphin, antenna, satellite dish, utility pole, manhole, survey point, surveillance, water tap, water well, petroleum well, mineshaft, charge point, beehive, telescope, works, kiln, street cabinet, snow cannon, telephone box, fuel pump, adit) to the CONST/SYMBOL section of `stylesheets/include/man_made.oss` (spec: Style definitions for new man made types) and verify no symbol name collides with existing symbols

## 3. Verification

- [x] 3.1 Verify the stylesheets parse by running the style validation on `map.ost` + `standard.oss` and confirming no type/style parse errors
- [x] 3.2 Verify the build compiles without errors (`cmake --build build` for the affected targets)
- [x] 3.3 Verify existing tests still pass (`cd build && ctest -j 2 --output-on-failure`)
- [x] 3.4 Re-import a test database and verify the new types exist in the `TypeConfig` (e.g. via `DumpData` or a type listing) and that ways render with the expected lines
