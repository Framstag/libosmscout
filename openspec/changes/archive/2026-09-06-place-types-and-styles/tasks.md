## 1. Place Type Definitions

- [x] 1.1 Add 17 new `place_*` types to `stylesheets/map.ost` in the place section, alphabetically ordered, with element types per the OSM wiki: `place_province`, `place_district`, `place_subdistrict` (NODE); `place_municipality`, `place_borough`, `place_quarter`, `place_neighbourhood`, `place_plot`, `place_isolated_dwelling` (NODE AREA RELATION MULTIPOLYGON); `place_archipelago` (AREA RELATION MULTIPOLYGON); `place_ocean` (NODE); `place_farm`, `place_allotments`, `place_city_block`, `place_square`, `place_polder`, `place_sea` (NODE AREA). Verify: `OSTAndOSSTest stylesheets/map.ost stylesheets/standard.oss` reports OST OK (spec: Administrative/Urban/Rural/Other named place types).
- [x] 1.2 Add the remaining 3 documented `place_*` types to `stylesheets/map.ost`: `place_cadastral_community` (NODE AREA, `{Name, NameAlt, IsIn}`, ADMIN_REGION), `place_subcounty` (NODE AREA RELATION, MULTIPOLYGON ADMIN_REGION), `place_building_complex` (NODE AREA RELATION, MULTIPOLYGON). Verify: `grep -c "place_cadastral_community\|place_subcounty\|place_building_complex" stylesheets/map.ost` returns 3 (spec: Remaining documented place types).
- [x] 1.3 Verify no discouraged/undocumented/proposal values were added (`place_yes`, `place_civil_parish`, `place_subdivision`, `place_block`, `place_field`, `place_department`, `place_zone`, `place_township`, `place_ward`, `place_community`, `place_camp` absent). Verify: `grep -c "place_yes\|place_civil_parish\|place_subdivision\|place_block\|place_field\|place_department\|place_zone\|place_township\|place_ward\|place_community\|place_camp" stylesheets/map.ost` returns 0 (spec: Discouraged and undocumented place values are not defined).

## 2. Place Style Definitions

- [x] 2.1 Add label priority constants to the CONST block of `stylesheets/include/place.oss`: `labelPrioArchipelago` (3), `labelPrioPolder` (5), `labelPrioSquare` (14), `labelPrioProvince` (4), `labelPrioDistrict`/`labelPrioSubdistrict`/`labelPrioMunicipality` (5), `labelPrioBorough`/`labelPrioQuarter` (11), `labelPrioNeighbourhood`/`labelPrioCityBlock` (12), `labelPrioPlot` (13), `labelPrioIsolatedDwelling`/`labelPrioFarm`/`labelPrioAllotments` (13). Verify: `OSTAndOSSTest` reports OSS OK (spec: Label rendering for new place types).
- [x] 2.2 Add NODE.TEXT/AREA.TEXT label rules for the 17 new types at the appropriate magnification blocks: administrative types (`province`, `district`, `subdistrict`, `municipality`) in `[MAG region-city]`/`[MAG county-city]`; settlement types (`borough`, `quarter`, `neighbourhood`, `city_block`, `plot`, `isolated_dwelling`, `farm`, `allotments`) in `[MAG suburb-veryClose]`/`[MAG suburb-suburb]`; small named places (`square`, `city_block`, `plot`) in `[MAG closer-]`; water/landmass types (`archipelago`, `polder`, `sea`, `ocean`) in `[MAG region-veryClose]`; extend the `[MAG region-city]` AREA.TEXT list with the new settlement types. Verify: `OSTAndOSSTest --analyze` shows all new types with style, none in "without style" lists (spec: Label rendering for new place types).
- [x] 2.3 Add label rules for the 3 remaining types: `place_cadastral_community` and `place_subcounty` NODE.TEXT in the `[MAG county-city]` block, `place_building_complex` NODE.TEXT in the `[MAG closer-]` block. Verify: `OSTAndOSSTest --analyze` shows the 3 types with style (spec: Label rendering for new place types).

## 3. Verification

- [x] 3.1 Run `OSTAndOSSTest` against `map.ost` + all stylesheets (`standard.oss`, `cycle.oss`, `winter-sports.oss`, `railways.oss`, `motorways.oss`, `public-transport.oss`). Verify: all report OK (rule: stylesheets load without errors).
- [x] 3.2 Run `ctest -R CheckStyleSheet` in the build directory. Verify: all pass (rule: existing tests still pass).
- [x] 3.3 Build the project (`cmake --build build`). Verify: compiles without errors (rule: build compiles without errors).
- [x] 3.4 Re-run `OSTAndOSSTest` and `ctest -R CheckStyleSheet` after adding the 3 remaining types. Verify: all report OK and all tests pass (spec: Remaining documented place types; rule: existing tests still pass).
