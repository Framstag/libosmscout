# Tasks

## 1. Worker shutdown and failure handling (spec: async-worker-lifetime)

- [x] 1.1 In `libosmscout/include/osmscout/async/AsyncWorker.h` and `libosmscout/src/osmscout/async/AsyncWorker.cpp`, add a public `Stop()` that stops the queue and then joins the worker thread (detaching when it is called from the worker thread itself), harmless when called twice, and have `~AsyncWorker()` use it. Document on the method that a derived class whose jobs use its members must call it before those members are destroyed. Verify: the library builds and the existing async tests pass.
- [x] 1.2 In the same files, make a failing job harmless: catch exceptions where the job's promise lives (`Async<T>`) and log the failure, resolving the promise with a default-constructed result, and add a backstop catch in `AsyncWorker::Loop()` so no job can escape the worker thread. Verify: the library builds and `AsyncProcessingTest` passes.
- [x] 1.3 Call `Stop()` as the first statement of the destructors of the workers whose jobs use their members: `MapManager` (`libosmscout-client/include/osmscoutclient/MapManager.h`, `MapManager.cpp`), `DBThread` (`libosmscout-client/src/osmscoutclient/DBThread.cpp`) and `MapDownloadService` (`libosmscout-client/src/osmscoutclient/MapDownloadService.cpp`), each with a comment naming the reason. Verify: the client library builds and the existing client tests pass.

## 2. Tests (spec: async-worker-lifetime)

- [x] 2.1 In `Tests/src/AsyncWorkerTest.cpp`, add a test worker whose member records its destruction into state owned by the test, and a job that reports whether it ran after that: assert that destroying the worker waits for the running job and that the job never observes destroyed members. Verify: the test passes, and it fails when the worker is not stopped before its members are destroyed (checked in that direction while developing).
- [x] 2.2 Extend the test with the cases that must stay harmless or unchanged: a job that was queued when the worker was destroyed still runs (the queue drains) and does so without observing destroyed members, the shutdown can be called twice, and a worker that deletes itself from its own job does not deadlock. Verify: the test passes.
- [x] 2.3 Extend the test with the failure cases: a job that throws is logged and the process keeps running, a later job still runs and its result reaches its caller, and the caller of the failed job is released with the default value instead of waiting forever. Verify: the test passes.

## 3. Verification

- [x] 3.1 Verify the C++ test suite still passes. Verify: `cd build && ctest -j 2 --output-on-failure` reports no new failures.
- [x] 3.2 Verify the abort that motivated the change is gone. Verify: the JUnit reproduction that aborted with SIGABRT before the change (`Launch com.framstag.libosmscout.client.SearchReproTest` against a freshly built native library) completes without a crash, and `cd JavaScout && mvn test -Dnative.lib.dir=<build>/lib` reports no new failures. **Result:** the reproduction completes cleanly in three consecutive runs (exit 0, no `bad_alloc`, 2 tests successful, 1 aborted by its assumption) where it aborted with SIGABRT before. The Java suite now runs to completion instead of dying with the fork. **Observation, not caused by this change:** `OSMScoutClientBasemapConfigTest` (from the earlier basemap change) is flaky in this environment - its render-after-basemap-reload assertion polls for 20 s and occasionally sees no render. Measured on both libraries: pre-change 2 of 3 runs failed, with this change 2 of 4 - so the flakiness is pre-existing and belongs to that test, not here. `OSMScoutClientStyleTest` passes on its own and only fails in a full-suite run after another class left the process-wide client open (the known test-hygiene issue).
- [x] 3.3 Verify the full build compiles without errors and without new warnings for the affected targets. Verify: the CMake build (`cmake --build build`) and the Meson build (`meson compile -C build-meson`) both complete.
- [x] 3.4 Verify the changed files against the project rules. Verify: `clang-tidy` with the repository `.clang-tidy` reports no new findings for the changed files, and they have no uncrustify drift. **Result:** clang-tidy reports no finding in any added region. `AsyncWorker.cpp` went from 5 findings with the change to 1 (the remaining one is the file's pre-existing missing `Breaker` include; the three mine - `std::exception`, `std::this_thread::get_id`, `osmscout::log` - are fixed by adding the includes the checker asked for). `AsyncWorkerTest.cpp` reports 11 findings, all on pre-existing lines; the two `misc-include-cleaner` reports on the chrono literals are silenced with a justification because the operators do live in the included `<chrono>`. Uncrustify drift: these files are not uncrustify-clean on `master` either (AsyncWorker.h 12, AsyncWorker.cpp 76, MapManager.cpp 253, DBThread.cpp 1115, MapDownloadService.cpp 605, AsyncWorkerTest.cpp 17 lines of drift) and the script that checks it is not enforced by any workflow; the added code follows the local style of each file.
