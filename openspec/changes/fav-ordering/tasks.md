# Tasks

## 1. Group order in the favorite service

Spec: `fav-group-order`. Design: D1, D4, D7, D8.

- [x] 1.1 Replace the name-keyed group container in `libosmscout-client/include/osmscoutclient/FavoriteLocationService.h` with an ordered collection that keeps the stored sequence, and adapt every group accessor (`GetGroups`, `AddGroup`, `DeleteGroup`, `RenameGroup`, `GetFavorites`, `AddFavorite`, `DeleteFavorite`, `RenameFavorite`, `MoveFavorite`, `SetStarred`, `IsStarred`, `SetGroupColor`, `GetGroupColor`) to it; verify `cmake --build build` and `meson compile -C build` both succeed without warnings.
- [x] 1.2 Make `AddGroup` append at the end and `RenameGroup` keep the group's position, and verify the new cases in `Tests/src/FavoriteLocationServiceTest.cpp` for append, rename-in-place and delete-keeps-order pass (`ctest -R FavoriteLocationServiceTest --output-on-failure`).
- [x] 1.3 Add `MoveGroup(name, newIndex)` with the target-position-after-removal semantics and clamping, and verify the `fav-group-order` move scenarios (front, middle, end, beyond end, own position, single group, unknown group) pass as new cases in `Tests/src/FavoriteLocationServiceTest.cpp`.
- [x] 1.4 Add unit tests that a moved group keeps its name, its attributes and its favorites in order, and that the chosen order survives a save and reload cycle by a new service instance; verify they pass.
- [x] 1.5 Add the file format version: write it on every save; on load treat a missing version as the pre-version form (`groups` keyed by name, name-sorted fallback order) and the version this change writes as the ordered form; verify new cases in `Tests/src/FavoriteLocationServiceTest.cpp` for a pre-version file, a fresh versioned file and a versioned file with an order, including that pre-version content is rewritten in the versioned form.
- [x] 1.6 Verify the existing tests still pass (`ctest -R "FavoriteLocationServiceTest|FavoriteStoreTest" --output-on-failure`), adjusting only cases whose expectation was the old alphabetical order.
- [x] 1.7 Write `Documentation/FavoritesFileFormat.md` describing the persisted file: the version field and the versions it can carry, the ordered group list, groups with their names, attributes and favorite lists, favorites with name, coordinates, attributes and the star order value, the pre-version form, an example of a versioned document, and the compatibility policy (which versions are read, that a newer version is read as nothing and never overwritten, and that reading the pre-version form is a compatibility path with a recorded end); verify every statement against a file written by a test run and against the loader's behaviour, and point the public API documentation of `libosmscout-client/include/osmscoutclient/FavoriteLocationService.h` at this document.
- [x] 1.8 Make a file whose version is newer than the highest known version report readable state instead of groups: the service exposes the version found and whether it is supported, reports no groups for it, and fails any save over that path; verify new cases for the state read, the empty group read and the failed save that leaves the file unchanged.

## 2. Moving a fav into another group

Spec: `fav-location-service` (added requirement). Design: D3.

- [x] 2.1 Add `MoveFavoriteToGroup(srcGroup, favName, dstGroup, newIndex)` to the service: collision check before any mutation, removal plus insertion under one write lock, clamping and same-group no-op; verify `cmake --build build` succeeds.
- [x] 2.2 Add unit tests in `Tests/src/FavoriteLocationServiceTest.cpp` for the success scenarios of the new requirement (into the middle, to the end, into an empty group, into the same group) and verify they pass.
- [x] 2.3 Add unit tests that the moved fav keeps coordinates, attributes, its star and its star position, and that both groups keep the relative order of their remaining favs; verify they pass.
- [x] 2.4 Add unit tests for the failure scenarios (destination name collision, unknown source group, unknown destination group, unknown fav) asserting both groups are unchanged; verify they pass.
- [x] 2.5 Add a unit test that the new group assignment survives a save and reload cycle; verify it passes.

## 3. Starred order

Spec: `fav-star-order`. Design: D2.

- [x] 3.1 Add the ordered starred reader to the service, returning each starred fav together with its group, sorted by the store-owned star position with a defined fallback for missing, unknown or duplicated values; verify `cmake --build build` succeeds.
- [x] 3.2 Add `MoveStarred(groupName, favName, newIndex)` with the target-position-after-removal semantics and clamping over the derived starred sequence, and verify `cmake --build build` succeeds.
- [x] 3.3 Add unit tests in `Tests/src/FavoriteLocationServiceTest.cpp` for the reader scenarios (chosen order, cross-group entries naming their group, only starred favs appear, empty store) and verify they pass.
- [x] 3.4 Add unit tests for the move scenarios (front, middle, beyond end, own position, cross-group, an unstarred fav fails, unknown group/fav fail, groups and fav order untouched) and verify they pass.
- [x] 3.5 Make unstarring drop the star position and starring append at the end, and verify the lifecycle cases (append on star, star again keeps position, star after unstar appends, unstar a non-starred fav changes nothing) pass.
- [x] 3.6 Add unit tests that the arranged starred order survives a save and reload cycle, and that a fav moved to another group keeps its star position; verify they pass.
- [x] 3.7 Add a load-tolerance unit test with a hand-written file carrying a missing and a duplicated star position, asserting a total order and that the following save writes store-owned values; verify it passes.

## 4. Store ownership of the new operations

Spec: `fav-location-store` (modified and added requirements). Design: D1, D4.

- [x] 4.1 Forward the group move, the cross-group fav move, the starred move and both new readers through `FavoriteStore` under its existing lock, returning the documented no-store results when no store is loaded; verify `cmake --build build` succeeds.
- [x] 4.2 Make the wholesale replacement rebuild the store in the snapshot's group order and star order instead of re-adding in a derived order, and verify the new `Tests/src/FavoriteStoreTest.cpp` cases "supplied group order is kept", "orders round-trip through the file" and "repeated replacements do not mix orders" pass.
- [x] 4.3 Add `Tests/src/FavoriteStoreTest.cpp` cases for the forwarded positioning operations, for a refused cross-group move reported through the store, and for the readers being empty without a store; verify they pass.
- [x] 4.4 Verify the existing concurrency and replacement tests still pass (`ctest -R FavoriteStoreTest --output-on-failure`).
- [x] 4.5 Expose the file version and its support state through `FavoriteStore`, and make both the save and the persisting replacement fail while the version is unsupported, leaving the file unchanged; verify new `Tests/src/FavoriteStoreTest.cpp` cases for the reported state, the refused save and the refused replacement with the file compared before and after.
- [x] 4.6 Verify both build systems still configure and build from scratch: done for both (`cmake -B build-verify -DCMAKE_BUILD_TYPE=Release` + build, `meson setup build-meson-verify` + compile), both exit 0. No warning comes from a file this change touched; the 66 CMake warnings and the Meson warnings are pre-existing (`libosmscout-client-qt/src/osmscoutclientqt/InputHandler.cpp` 30, `MapWidget.cpp` 9, vendored `nanosvg.h` 8, Doxygen config notices); no new warning appears for the favorites code, and both build systems do compile and pack the new `StarredFavoriteLocation` class (verified in the built jars).

## 5. Java bindings

Spec: `fav-location-java-bindings` (added requirements). Design: D5.

- [x] 5.1 Add `StarredFavoriteLocation` with `groupName` and `favorite` to `libosmscout-client-java/java/com/framstag/libosmscout/client/`, and verify it is compiled by the Java build of the client bindings.
- [x] 5.2 Add the `moveGroup`, `moveFavoriteToGroup`, `moveStarredFavorite` and `getStarredFavorites` declarations with Javadoc to `OSMScoutClient.java` and their JNI entry points in `libosmscout-client-java/src/OSMScoutClient.cpp`, mapping a negative target position to the first position and returning arrays in stored order; verify `cmake --build build` succeeds and the JNI library links.
- [x] 5.3 Add binding-level tests (JNI round-trip through the existing client test harness, or a focused Java test where one exists) covering group move, cross-group move with and without a name collision, starred move, unstar-appends and the arranged orders reaching the file; verify they pass, or record in the change why no Java-level harness exists and cover the same paths through the C++ store tests.
- [x] 5.4 Verify a Java-level call sequence that starts without a loaded store reports the documented no-store results for the new methods instead of faulting.
- [x] 5.5 Add `getFavoriteFileFormatVersion` and `isFavoriteFileFormatSupported` declarations and JNI entry points, returning `-1` and false without a loaded file, and verify the binding tests for an unsupported file (state reported, `getFavoriteGroups` empty, `saveFavoriteLocations` returns false and the file is unchanged) and for a pre-version and a current file (both reported as supported) pass.

## 6. JavaScout dialog

Spec: `javascout-fav-location-ui` (added requirements). Design: D6.

- [x] 6.1 Show the group list in stored order and add move-towards-beginning / move-towards-end actions for the selected group in `JavaScout/src/main/java/com/framstag/libosmscout/FavLocationDialog.java`, disabled at the borders; verified by driving the built dialog in `FavLocationDialogOrderingTest` (stored order shown, last group moved towards the beginning, first group moved towards the end, both border actions disabled) under `xvfb-run`, and the order is asserted in the store after each move.
- [x] 6.2 Make `FavoritePickerDialog` and the favorites overlay follow the group order; verified with `FavLocationDialogOrderingTest.theFavoritePickerFollowsTheSameGroupOrder`, which builds both the management dialog and the picker on the same store and asserts the identical group sequence. The favorites overlay (`SearchOverlay`) cannot be constructed in a test without the main controller; it was verified by reading: it builds its tree from `client.getFavoriteGroups()` in array order (`SearchOverlay.java` around line 635) and sorts nothing, like the picker.
- [x] 6.3 Add the move-to-group action for the selected fav, reloading both group lists after a successful move and refreshing the map markers, and report a refused move (destination holds the same name) without changing either list; verified in `FavLocationDialogOrderingTest` that the action exists for a selected favorite and that a dialog reopened after the move shows the favorite under the destination group and no longer under the source group. The refused move is reported by the action and its store behavior (failure, both groups unchanged) is asserted in `OSMScoutClientFavoriteOrderingTest` and in the store tests; the modal choice dialog the action opens cannot be answered from a test.
- [x] 6.4 Add the starred favorites list in stored order, each entry showing its owning group, with move-towards-beginning / move-towards-end actions, and make starring append to the end and unstarring remove the entry; verified in `FavLocationDialogOrderingTest` by driving the built dialog (entries in store order with their group, moved towards the beginning with the store following, border action disabled, starring appended at the end, unstarring removed the entry).
- [x] 6.5 Show a notice when the loaded file carries a version this client does not support, instead of an empty favorites list, refuse to save and report the refusal, and offer no way to overwrite the file; verified in `FavLocationDialogOrderingTest.anUnsupportedFileIsReportedAndCannotBeSaved` (notice naming the version, no groups shown, Save and the ordering actions disabled, the file byte-identical after opening and closing the dialog).

## 7. Integration verification

- [x] 7.1 Run the full test suite for both build systems: CMake `ctest -j 2 --output-on-failure` 136/136 passed, Meson `meson test --timeout-multiplier 2 --print-errorlogs` 136/136 OK, both re-run after the final tidy-ups. The JavaScout module adds 8 JNI tests and 5 dialog tests (the latter under `xvfb-run`), all green. No test failure and no warning attributable to this change; the build warnings reported under 4.6 are pre-existing.
- [x] 7.2 Run the formatter and static analysis on every touched C++ file: `clang-tidy -p build` was run on `FavoriteLocationService.cpp`, `FavoriteStore.cpp`, `OSMScoutClient.cpp` and both test files, and the findings this change introduced were fixed (new file-local helpers moved into anonymous namespaces for internal linkage, direct includes added, the JSON key literals const-qualified, clamping rewritten with `std::min`); `FavoriteStore.cpp` is now clean. What remains are categories the untouched files share (`cppcoreguidelines-pro-bounds-avoid-unchecked-container-access`, `readability-qualified-auto`, `modernize-use-ranges`, magic numbers in tests, the JNI file's C-vararg `NewStringUTF` pattern, and `cppcoreguidelines-special-member-functions` on the pre-existing `FavoriteStore` destructor). `uncrustify --check` is not a usable gate here: with Uncrustify 0.83.0 and the repository config it reports a change for every file tried, including untouched ones. Both observations are recorded in `TODO.md` as a follow-up rather than fixed in this change.
- [x] 7.3 Verify the specs' scenarios are traceable to the tasks above and that no scenario of `fav-group-order`, `fav-star-order`, `fav-location-service`, `fav-location-store`, `fav-location-java-bindings` or `javascout-fav-location-ui` is left without a task, including the format-version, unsupported-version and pre-version compatibility scenarios. Two scenarios had no direct assertion and were closed while checking: the Java scenario that a moved group and a moved favorite keep their data (now `OSMScoutClientFavoriteOrderingTest.aMovedGroupAndAMovedFavoriteKeepTheirData`: group color, favorite attributes and star intact) and the UI scenario that reordering stars does not reorder the groups (now asserted in `FavLocationDialogOrderingTest.theStarredListFollowsTheStoreOrderAndItsActionsReorderIt`). The two scenarios that cannot be driven automatically are recorded in their tasks with the reason: the favorites overlay of `SearchOverlay` (verified by reading, `SearchOverlay.java` around line 635) and the modal choice dialog of the move-to-group action (its store effect is asserted through the client).
- [x] 7.4 Update `AGENTS.md` and `TODO.md`: the root `Documentation/` row gained `FavoritesFileFormat.md`, `libosmscout-client-java/AGENTS.md` now counts 20 Java API source files, and `JavaScout/AGENTS.md` lists the two new test classes and notes the `xvfb-run` requirement for the dialog test. Problems found but not initiated by this change went into `TODO.md` instead of being fixed here.
- [x] 7.5 Record the deprecation of the pre-version read path in `TODO.md`: entry "Drop the read path for the pre-version favorites file once enough releases have shipped" names this change, states the gate as adoption rather than a date, points at the version 0 branch in `FavoriteLocationService.cpp`, and requires the format document and its compatibility table to be updated in the same step.
