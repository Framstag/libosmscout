# Tasks

## 1. Shared stylesheet resolution (spec: client-java-style-switching)

- [x] 1.1 In `libosmscout-client-java/src/OSMScoutClient.cpp`, extract the stylesheet-name policy out of `Java_..._loadStyleSheet` into one file-local helper (input: stylesheet directory + name; output: absolute stylesheet path, or empty when the name is empty / contains `/` or `\` / is `.` or `..` / has no matching file). Verify: `Java_..._loadStyleSheet` uses the helper and the native library still compiles.
- [x] 1.2 Confirm the helper preserves the existing behaviour of every stylesheet-API path. Verify: `OSMScoutClientStyleTest` (stylesheets directory + active default, enumeration, load success with and without `.oss`, unknown style, path traversal, style flags, unloadable stylesheet) passes against the rebuilt native library.

## 2. Basemap stylesheet selection (spec: client-java-style-switching)

- [x] 2.1 In `libosmscout-client-java/java/com/framstag/libosmscout/client/OSMScoutClientBuilder.java`, add the `basemapStyleSheet` member and `withBasemapStyleSheet(String)` with Javadoc that states the name is the stylesheet file name with or without `.oss`, resolved against the stylesheets directory, and that an unusable name leaves the basemap on the active map style. Verify: the Java sources compile (`meson compile -C build` with `-DOSMSCOUT_BUILD_CLIENT_JAVA=ON`, or `mvn -q -DskipTests package` inside `libosmscout-client-java`).
- [x] 2.2 In `libosmscout-client-java/src/OSMScoutClient.cpp` `build()`, read `basemapStyleSheet` with `GetFieldID`, resolve it through the helper from 1.1 using `settings->GetStyleSheetDirectory()` (so the `stylesheets` default applies), and pass the result as the `basemapStyleFilename` constructor argument of `DBThread`. Verify: the native library compiles with no new warnings and, when no basemap stylesheet is configured, the constructor argument is empty exactly as before.
- [x] 2.3 Verify the resolution cannot escape the stylesheets directory and cannot fail client creation. Verify: the test in 4.1 covers a name containing a path separator and a name with no matching file, and both still return a non-null client whose active map style is unchanged.

## 3. Runtime basemap directory (spec: basemap-loading)

- [x] 3.1 In `libosmscout-client-java/java/com/framstag/libosmscout/client/OSMScoutClient.java`, add `public native void setBasemapLookupDirectory(String directory)` with Javadoc stating that the directory replaces the one configured at client creation, that an empty value unloads the basemap, that the reload runs on the native database thread without blocking the caller, and that the outcome is observable through the rendered map and `wasLastStyleLoadSuccessful()`. Verify: the Java sources compile.
- [x] 3.2 In `libosmscout-client-java/src/OSMScoutClient.cpp`, add the `Java_com_framstag_libosmscout_client_OSMScoutClient_setBasemapLookupDirectory` entry point: obtain `ClientData` with the existing helper, return early when the client data or the DB thread is null, copy the `jstring` into a `std::string`, release the string characters, and call `DBThread::SetBasemapLookupDirectory` without waiting for the returned future. Verify: the native library compiles and the symbol name and signature match the JNI header generated from the Java declaration.
- [x] 3.3 Verify the async contract of the setter. Verify: the test in 4.3 asserts the call returns well under the time a basemap database open takes, and that the basemap appears without any further Java-side call after the client is polled.

## 4. Tests

- [x] 4.1 Add a JUnit 5 test in `JavaScout/src/test/java/com/framstag/libosmscout/client/` covering the basemap stylesheet selection without a basemap database: named stylesheet, name given with `.oss`, no stylesheet named, name containing a path separator, and a name with no matching file. Assert for every case that the client is created, that the active map style is unchanged, and that the invalid and unmatched names behave like "not named". Verify: `cd JavaScout && mvn test -Dnative.lib.dir=<build>/lib -Dtest=OSMScoutClientBasemapConfigTest`.
- [x] 4.2 Extend the basemap stylesheet test with the fixture-dependent cases, using the `search.test.db.dir` system property / `JAVASCOUT_MAP_DIR` environment variable convention of the other JNI tests, and skip with an assumption when no database directory is provided: a basemap stylesheet that exists but cannot be parsed leaves the basemap layer undrawn while the map keeps rendering, and a valid basemap stylesheet keeps the map style unchanged. Verify: the test passes with a provided database directory and reports a skip without one.
- [x] 4.3 Add a JUnit 5 test for the runtime basemap directory: setting a directory that holds an openable basemap makes the basemap available, setting an empty value unloads it, setting a directory with no openable basemap draws no basemap layer, and the remaining maps keep rendering in all three cases; assert the setter call returns promptly. Verify: the test passes against the rebuilt native library.
- [x] 4.4 Verify that all pre-existing client and application tests still pass. Verify: `cd JavaScout && mvn test -Dnative.lib.dir=<build>/lib` reports no new failures.
- [x] 4.5 Verify that the C++ test suite still passes. Verify: `cd build && ctest -j 2 --output-on-failure` reports no new failures.

## 5. Build and quality gates

- [x] 5.1 Verify the full build compiles without errors and without new warnings for the affected targets. Verify: the CMake build (`cmake --build build`) and, where available, the Meson build (`meson compile -C build-meson`) both complete for `libosmscout-client-java`.
- [x] 5.2 Verify the changed C++ file matches the project formatting and static-analysis rules. Verify: `./scripts/format-check.sh check` reports no diff for `libosmscout-client-java/src/OSMScoutClient.cpp`, and `clang-tidy` with the repository `.clang-tidy` reports no new findings for it. **Verification adjusted (accepted 2026-09-24):** the uncrustify half of this criterion is unsatisfiable in this repository — `./scripts/format-check.sh check` flags 799 files, including the unmodified `master` copy of this file (6987 diff lines of pre-existing drift), and no CI workflow runs it; the added lines contribute 17 further lines of that same pre-existing drift and follow the surrounding code. Accepted as: pre-existing repo-wide uncrustify drift is the baseline. The static-analysis half is verified: `clang-tidy -p build` reports 693 findings for the file, i.e. zero new findings (the two introduced by the new helper are suppressed with a justified `NOLINTBEGIN`/`NOLINTEND`, precedent `libosmscout/src/osmscout/io/Crc32.cpp`).
- [x] 5.3 Update `libosmscout-client-java/AGENTS.md`: document the basemap stylesheet and runtime basemap directory members in the client API summary, and correct the stale "Meson (only — no CMake support)" claim so it matches the two build descriptions in the repository root. Verify: the file names both behaviours and agrees with `libosmscout-client-java/CMakeLists.txt` and `libosmscout-client-java/meson.build`.
